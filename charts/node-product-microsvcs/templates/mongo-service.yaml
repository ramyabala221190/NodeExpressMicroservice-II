apiVersion: v1
kind: Service
metadata:
 name: {{.Values.services.mongo.name}}-service-{{.Values.global.environment}}  #service name
spec:
 type: {{.Values.services.mongo.type}}  #type of service
 selector:
    app: {{.Values.services.mongo.name}}-deployment-{{.Values.global.environment}} #selecting the pods
 ports: 
   - port: {{.Values.services.mongo.port}}
     targetPort: {{.Values.app.mongo.port}} # this is the container port to which requests are forwarded to 